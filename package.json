{
  "name": "recitation-cicd-demo",
  "version": "1.0.0",
  "description": "Demo app for CICD recitation using GitHub Actions, Tailwind CSS, and Jest",
  "private": true,
  "scripts": {
    "start": "npx serve . -s",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@babel/core": "^7.22.0",
    "@babel/preset-env": "^7.22.0",
    "babel-jest": "^29.6.0",
    "jest": "^29.6.0",
    "jest-environment-jsdom": "^29.6.0",
    "serve": "^14.2.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "transform": {
      "^.+\\.js$": "babel-jest"
    }
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  }
}
